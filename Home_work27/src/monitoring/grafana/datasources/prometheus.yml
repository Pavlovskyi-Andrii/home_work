apiVersion: 1

# Конфигурация источников данных для Grafana
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true